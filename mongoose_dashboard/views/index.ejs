<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" type="text/css" href="/bootstrap.css">

    <title>Das Crows</title>
</head>
<body>
    <div class="container shadow p-3 mb-5 bg-white rounded">
        <div class="text-center shadow p-3 mb-5 bg-white rounded">
            <h1>Check Out This Murder of Crows!</h1>
            <small class="text-muted">dey scary</small>
        </div>
        <% for(var i = 0 ; i < crows.length ; i++) {%>
          <p>
              <a href="/crows/<%= crows[i]._id%>" class="h2"><%= crows[i].name%></a>
              <p class="text-info"> *  <%= crows[i].about%></p>
          </p>
        <% } %>

        <a href="/crows/new" class="btn btn-dark mt-3">Add a Crow!</a>
    </div>
</body>
</html>