<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" type="text/css" href="/bootstrap.css">

    <title>Das Crow</title>
</head>
<body>
    <div class="container shadow p-3 mb-5 bg-white rounded">
        <% for(var i = 0 ; i < crow.length ; i++) { %>
        <div class="text-center shadow p-3 mb-5 bg-white rounded">
        <a href='/' class="float-right btn btn-dark">Home</a>
            <h1>Let's edit <%= crow[i].name%>!</h1>
            <small>They've changed a bit since they were first entered, and we'd like to stay up to date</small>
        </div>
        
        <form action='/crows/<%= crow[i]._id%>' method='POST'>
            <div class="form-group">
              <label for="name">Name:</label>

                <% if(messages.name) { %>
                    <% for (var x of messages.name) { %>
                    <small class="text-danger"><%= x %></small>
                    <% } %>
                <% } %>

              <input type="text" class="form-control" id="name" aria-describedby="enterName" name="name" value="<%= crow[i].name%>">
              <small id="enterName" class="form-text text-muted">Has their name changed?</small>
            </div>
            <div class="form-group">
              <label for="about">About:</label>

                <% if(messages.about) { %>
                    <% for (var x of messages.about) { %>
                    <small class="text-danger"><%= x %></small>
                    <% } %>
                <% } %>

              <textarea type="text" rows=5 class="form-control" id="about" name="about" aria-describedby="aboutCrow" placeholder="Tell us about this crow..."><%= crow[i].about%></textarea>
              <small id="aboutCrow" class="form-text text-muted">What about their interests?</small>
            </div>
            <input type="submit" class="btn btn-dark" value="Update <%= crow[i].name%>'s Profile!">
        </form>
        <% } %>


    </div>
</body>
</html>