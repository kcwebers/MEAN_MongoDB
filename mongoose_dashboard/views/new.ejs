<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" type="text/css" href="/bootstrap.css">

    <title>Das Crows</title>
</head>
<body>
    <div class="container shadow p-3 mb-5 bg-white rounded">
    <div class="text-center shadow p-3 mb-5 bg-white rounded">
    <a href='/' class="float-right btn btn-dark">Home</a>
        <h1>Let's add a new Crow!</h1>
        <small class="text-muted">make sure he scary</small>
    </div>
        <form action='/crows' method='POST'>
            <div class="form-group">
              <label for="name">Name:</label>

                <% if(messages.name) { %>
                    <% for (var x of messages.name) { %>
                    <small class="text-danger"><%= x %></small>
                    <% } %>
                <% } %>

              <input type="text" class="form-control" id="name" aria-describedby="enterName" name="name" placeholder="Enter Your Name">
              <small id="enterName" class="form-text text-muted">What is this crow's name?</small>
            </div>
            <div class="form-group">
              <label for="about">About:</label>

                <% if(messages.about) { %>
                    <% for (var x of messages.about) { %>
                    <small class="text-danger"><%= x %></small>
                    <% } %>
                <% } %>

              <textarea type="text" rows=5 class="form-control" id="about" name="about" aria-describedby="aboutCrow" placeholder="Tell us about this crow..."></textarea>
              <small id="aboutCrow" class="form-text text-muted">Do they have any interests? What are their goals? How's their family life?</small>
            </div>
            <input type="submit" class="btn btn-dark" value="Add My Crow to the Murder!">
        </form>

    </div>
</body>
</html>