<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" type="text/css" href="/bootstrap.css">

    <title>Das Quote</title>
</head>
<body>
    <div class="container shadow p-3 mb-5 bg-white rounded">
        <div class="text-center shadow p-3 mb-5 bg-white rounded">
            <h1>Welcome to Quoting Dojo</h1>
            <small>Appreciate our hilarious wordplay, if you please</small>
        </div>

        <form action='/add_quotes' method='POST'>
            <div class="form-group">
              <label for="quoter">Name:</label>
              <!-- <%- JSON.stringify(messages) %>  -->
                <% if(messages.quotes) { %>
                    <% for (var x of messages.quotes) { %>
                    <small class="text-danger"><%= x %></small>
                    <% } %>
                <% } %>
              <input type="text" class="form-control" id="quoter" aria-describedby="enterName" name="quoter" placeholder="Enter Your Name">
              <small id="enterName" class="form-text text-muted">Please enter your name!</small>
            </div>
            <div class="form-group">
              <label for="quote">Quote:</label>
              <!-- <%- JSON.stringify(messages) %>  -->
                <% if(messages.quotes) { %>
                    <% for (var x of messages.quotes) { %>
                    <small class="text-danger"><%= x %></small>
                    <% } %>
                <% } %>
              <textarea type="text" rows=5 class="form-control" id="quote" name="quote" aria-describedby="quoteEntry" placeholder="Your Quote Here..."></textarea>
              <small id="quoteEntry" class="form-text text-muted">Please enter your quote!</small>
            </div>
            <input type="submit" class="btn btn-light" value="Add My Quote!">
        </form>

        <a href="/quotes" class="btn btn-light mt-3">Skip To Quotes!</a>
    </div>
</body>
</html>